<%= form_for(@part_status) do |f| %>
  <% if @part_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@part_status.errors.count, "error") %> prohibited this part_status from being saved:</h2>

      <ul>
      <% @part_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :part_status %><br />
    <%= f.text_field :part_status %>
  </div>
  <div class="field">
    <%= f.label :status_description %><br />
    <%= f.text_area :status_description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
